#ifndef UTILS_H
#define UTILS_H

/// @brief Check wether a given string only contains numerical character (0->9).
/// @param s String to check.
/// @return 1 if the string only contains numerical values, 0 otherwise.
int str_is_num_only(char *s);

#endif